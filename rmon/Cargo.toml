[package]
name = "rmon"
version = "0.1.0"
edition = "2021"
description = "CLI Resource Monitor for macOS"
authors = ["Your Name"]

[dependencies]
# No external dependencies!

[profile.release]
opt-level = 3
lto = true
strip = true
